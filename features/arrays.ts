const carMaker: string[] = ['ford', 'toyota', 'chefy'];
const listDate = [new Date(), new Date()];

carMaker.map((car: string): string => {
  return car.toUpperCase();
});

const data = [];
data.push(new Date());
console.log(data);
